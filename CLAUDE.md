# Airbucks — Claude Instructions

## Talk less.

Terse, not verbose.

## After every completed feature

Commit. One commit per feature, with a clear message. Don't batch unrelated changes.

## Tests

Add tests for every new piece of logic. Test files live alongside source files (e.g. `engine.test.ts` next to `engine.ts`). Run `npm test` before committing to confirm nothing is broken.
